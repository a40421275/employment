# 就业平台 API 测试指南

## 项目概述
这是一个基于 Spring Boot 的就业平台后端系统，提供完整的招聘求职功能。

## 技术栈
- **后端框架**: Spring Boot 2.x
- **数据库**: MySQL
- **ORM**: Spring Data JPA
- **安全框架**: Spring Security
- **构建工具**: Maven

## 数据库配置
数据库连接配置在 `src/main/resources/application.properties` 中：
```properties
spring.datasource.url=jdbc:mysql://localhost:3306/employment_db
spring.datasource.username=root
spring.datasource.password=password
spring.jpa.hibernate.ddl-auto=update
```

## API 接口文档

### 1. 用户管理模块 (UserController)
**基础路径**: `/api/users`

#### 用户注册
- **URL**: `POST /register`
- **请求参数**:
  ```json
  {
    "username": "用户名",        // 字符串，必填，长度2-20字符
    "phone": "手机号",          // 字符串，必填，11位手机号格式
    "email": "邮箱",            // 字符串，必填，邮箱格式
    "password": "密码",         // 字符串，必填，长度6-20字符
    "userType": 1              // 整数，必填，1-求职者，2-企业用户，3-管理员
  }
  ```
- **响应参数**:
  ```json
  {
    "code": 200,                // 整数，状态码
    "message": "注册成功",       // 字符串，响应消息
    "data": {
      "id": 1,                  // 长整型，用户ID
      "username": "用户名",      // 字符串，用户名
      "phone": "手机号",        // 字符串，手机号
      "email": "邮箱",          // 字符串，邮箱
      "userType": 1,            // 整数，用户类型：1-求职者，2-企业用户，3-管理员
      "status": 1,              // 整数，用户状态：1-正常，2-禁用
      "authStatus": 0           // 整数，认证状态：0-未认证，1-已认证
    }
  }
  ```
- **功能**: 用户注册

#### 用户登录
- **URL**: `POST /login`
- **请求参数**:
  ```json
  {
    "account": "用户名/手机号/邮箱",  // 字符串，必填，支持用户名、手机号或邮箱登录
    "password": "密码"              // 字符串，必填，长度6-20字符
  }
  ```
- **响应参数**:
  ```json
  {
    "code": 200,                    // 整数，状态码
    "message": "登录成功",           // 字符串，响应消息
    "data": {
      "token": "JWT令牌",           // 字符串，JWT认证令牌
      "user": {
        "id": 1,                    // 长整型，用户ID
        "username": "用户名",        // 字符串，用户名
        "phone": "手机号",          // 字符串，手机号
        "email": "邮箱",            // 字符串，邮箱
        "userType": 1,              // 整数，用户类型：1-求职者，2-企业用户，3-管理员
        "status": 1,                // 整数，用户状态：1-正常，2-禁用
        "authStatus": 0             // 整数，认证状态：0-未认证，1-已认证
      }
    }
  }
  ```
- **功能**: 用户登录

#### 微信登录
- **URL**: `POST /login/wx`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | openid | 请求体 | String | 是 | 微信openid |
  | unionid | 请求体 | String | 否 | 微信unionid |
- **响应参数**:
  ```json
  {
    "code": 200,
    "message": "登录成功",
    "data": {
      "token": "JWT令牌",
      "user": {
        "id": 1,
        "username": "用户名",
        "phone": "手机号",
        "email": "邮箱",
        "userType": 1,
        "status": 1,
        "authStatus": 0
      }
    }
  }
  ```
- **功能**: 微信登录

#### 手机号登录
- **URL**: `POST /login/phone`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | phone | 请求体 | String | 是 | 手机号 |
  | verificationCode | 请求体 | String | 是 | 验证码 |
- **响应参数**:
  ```json
  {
    "code": 200,
    "message": "登录成功",
    "data": {
      "token": "JWT令牌",
      "user": {
        "id": 1,
        "username": "用户名",
        "phone": "手机号",
        "email": "邮箱",
        "userType": 1,
        "status": 1,
        "authStatus": 0
      }
    }
  }
  ```
- **功能**: 手机号登录

#### 获取用户信息
- **URL**: `GET /{id}`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | id | 路径 | Long | 是 | 用户ID |
- **响应参数**:
  ```json
  {
    "code": 200,
    "message": "获取成功",
    "data": {
      "id": 1,
      "username": "用户名",
      "phone": "手机号",
      "email": "邮箱",
      "userType": 1,
      "status": 1,
      "authStatus": 0
    }
  }
  ```
- **功能**: 获取指定用户信息

#### 更新用户信息
- **URL**: `PUT /{id}`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | id | 路径 | Long | 是 | 用户ID |
  | UserDTO | 请求体 | Object | 是 | 用户信息对象 |
- **响应参数**:
  ```json
  {
    "code": 200,
    "message": "更新成功",
    "data": {
      "id": 1,
      "username": "用户名",
      "phone": "手机号",
      "email": "邮箱",
      "userType": 1,
      "status": 1,
      "authStatus": 0
    }
  }
  ```
- **功能**: 更新用户信息

#### 获取用户资料
- **URL**: `GET /{id}/profile`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | id | 路径 | Long | 是 | 用户ID |
- **响应参数**:
  ```json
  {
    "code": 200,
    "message": "获取成功",
    "data": {
      "id": 1,
      "userId": 1,
      "realName": "真实姓名",
      "gender": 1,
      "birthday": "1990-01-01",
      "education": "本科",
      "workExperience": 3,
      "skills": "Java,Spring,MySQL",
      "selfIntroduction": "个人简介",
      "avatar": "头像URL",
      "resumeFile": "简历文件URL"
    }
  }
  ```
- **功能**: 获取用户资料

#### 更新用户资料
- **URL**: `PUT /{id}/profile`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | id | 路径 | Long | 是 | 用户ID |
  | UserProfileDTO | 请求体 | Object | 是 | 用户资料对象 |
- **响应参数**:
  ```json
  {
    "code": 200,
    "message": "更新成功",
    "data": {
      "id": 1,
      "userId": 1,
      "realName": "真实姓名",
      "gender": 1,
      "birthday": "1990-01-01",
      "education": "本科",
      "workExperience": 3,
      "skills": "Java,Spring,MySQL",
      "selfIntroduction": "个人简介"
    }
  }
  ```
- **功能**: 更新用户资料

#### 实名认证
- **URL**: `POST /{id}/realname-auth`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | id | 路径 | Long | 是 | 用户ID |
  | realName | 请求体 | String | 是 | 真实姓名 |
  | idCard | 请求体 | String | 是 | 身份证号 |
  | idCardFront | 请求体 | String | 是 | 身份证正面照片URL |
  | idCardBack | 请求体 | String | 是 | 身份证背面照片URL |
- **响应参数**:
  ```json
  {
    "code": 200,
    "message": "实名认证成功",
    "data": {
      "id": 1,
      "userId": 1,
      "realName": "真实姓名",
      "idCard": "身份证号",
      "authStatus": 1,
      "authTime": "2025-11-08T12:00:00"
    }
  }
  ```
- **功能**: 实名认证

#### 修改密码
- **URL**: `POST /{id}/change-password`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | id | 路径 | Long | 是 | 用户ID |
  | oldPassword | 请求体 | String | 是 | 旧密码 |
  | newPassword | 请求体 | String | 是 | 新密码 |
- **响应参数**:
  ```json
  {
    "code": 200,
    "message": "密码修改成功",
    "data": null
  }
  ```
- **功能**: 修改密码

#### 重置密码
- **URL**: `POST /reset-password`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | phone | 请求体 | String | 是 | 手机号 |
  | verificationCode | 请求体 | String | 是 | 验证码 |
  | newPassword | 请求体 | String | 是 | 新密码 |
- **响应参数**:
  ```json
  {
    "code": 200,
    "message": "密码重置成功",
    "data": null
  }
  ```
- **功能**: 重置密码

#### 发送验证码
- **URL**: `POST /send-verification-code`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | phone | 请求体 | String | 是 | 手机号 |
- **响应参数**:
  ```json
  {
    "code": 200,
    "message": "验证码发送成功",
    "data": {
      "phone": "13800138000",
      "expireTime": "2025-11-08T12:05:00"
    }
  }
  ```
- **功能**: 发送验证码

#### 分页查询用户列表
- **URL**: `GET /`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | page | 查询 | Integer | 否 | 页码，从0开始，默认0 |
  | size | 查询 | Integer | 否 | 每页大小，默认20 |
  | userType | 查询 | Integer | 否 | 用户类型：1-求职者，2-企业用户，3-管理员 |
  | status | 查询 | Integer | 否 | 用户状态：1-正常，2-禁用 |
  | authStatus | 查询 | Integer | 否 | 认证状态：0-未认证，1-已认证 |
- **响应参数**:
  ```json
  {
    "code": 200,
    "message": "查询成功",
    "data": {
      "content": [
        {
          "id": 1,
          "username": "用户名",
          "phone": "手机号",
          "email": "邮箱",
          "userType": 1,
          "status": 1,
          "authStatus": 0
        }
      ],
      "totalElements": 100,
      "totalPages": 5,
      "size": 20,
      "number": 0
    }
  }
  ```
- **功能**: 分页查询用户

#### 更新用户状态
- **URL**: `PUT /{id}/status`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | id | 路径 | Long | 是 | 用户ID |
  | status | 请求体 | Integer | 是 | 用户状态：1-正常，2-禁用 |
- **响应参数**:
  ```json
  {
    "code": 200,
    "message": "状态更新成功",
    "data": {
      "id": 1,
      "status": 1
    }
  }
  ```
- **功能**: 更新用户状态

#### 更新认证状态
- **URL**: `PUT /{id}/auth-status`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | id | 路径 | Long | 是 | 用户ID |
  | authStatus | 请求体 | Integer | 是 | 认证状态：0-未认证，1-已认证 |
- **响应参数**:
  ```json
  {
    "code": 200,
    "message": "认证状态更新成功",
    "data": {
      "id": 1,
      "authStatus": 1
    }
  }
  ```
- **功能**: 更新认证状态

#### 删除用户
- **URL**: `DELETE /{id}`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | id | 路径 | Long | 是 | 用户ID |
- **响应参数**:
  ```json
  {
    "code": 200,
    "message": "删除成功",
    "data": null
  }
  ```
- **功能**: 删除用户

### 2. 岗位管理模块 (JobController)
**基础路径**: `/api/jobs`

#### 创建岗位
- **URL**: `POST /`
- **请求参数**:
  ```json
  {
    "title": "岗位标题",
    "categoryId": 1,
    "companyId": 1,
    "companyName": "公司名称",
    "department": "部门",
    "jobType": 1, // 1-全职，2-兼职，3-实习
    "salaryMin": 8000,
    "salaryMax": 15000,
    "salaryUnit": "月",
    "workCity": "工作城市",
    "workAddress": "工作地址",
    "description": "岗位描述",
    "requirements": "任职要求",
    "benefits": "福利待遇",
    "contactInfo": "联系方式"
  }
  ```
- **响应参数**:
  ```json
  {
    "code": 200,
    "message": "创建成功",
    "data": {
      "id": 1,
      "title": "岗位标题",
      "categoryId": 1,
      "companyId": 1,
      "companyName": "公司名称",
      "jobType": 1,
      "salaryMin": 8000,
      "salaryMax": 15000,
      "salaryUnit": "月",
      "workCity": "工作城市",
      "status": 0
    }
  }
  ```
- **功能**: 创建岗位

#### 更新岗位
- **URL**: `PUT /{id}`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | id | 路径 | Long | 是 | 岗位ID |
  | JobDTO | 请求体 | Object | 是 | 岗位信息对象 |
- **功能**: 更新岗位

#### 删除岗位
- **URL**: `DELETE /{id}`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | id | 路径 | Long | 是 | 岗位ID |
- **功能**: 删除岗位

#### 获取岗位详情
- **URL**: `GET /{id}`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | id | 路径 | Long | 是 | 岗位ID |
- **功能**: 获取岗位详情

#### 分页查询岗位列表
- **URL**: `GET /`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | page | 查询 | Integer | 否 | 页码，从0开始，默认0 |
  | size | 查询 | Integer | 否 | 每页大小，默认20 |
  | sort | 查询 | String | 否 | 排序字段，如：createTime |
  | direction | 查询 | String | 否 | 排序方向：ASC/DESC，默认DESC |
- **响应参数**:
  ```json
  {
    "code": 200,
    "message": "查询成功",
    "data": {
      "content": [
        {
          "id": 1,
          "title": "岗位标题",
          "categoryId": 1,
          "companyName": "公司名称",
          "jobType": 1,
          "salaryMin": 8000,
          "salaryMax": 15000,
          "workCity": "工作城市",
          "status": 1
        }
      ],
      "totalElements": 100,
      "totalPages": 5,
      "size": 20,
      "number": 0
    }
  }
  ```
- **功能**: 分页查询岗位

#### 根据状态查询岗位
- **URL**: `GET /status/{status}`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | status | 路径 | Integer | 是 | 岗位状态：0-草稿，1-已发布，2-已下架，3-已归档 |
  | page | 查询 | Integer | 否 | 页码，从0开始，默认0 |
  | size | 查询 | Integer | 否 | 每页大小，默认20 |
- **功能**: 根据状态查询岗位

#### 根据分类查询岗位
- **URL**: `GET /category/{categoryId}`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | categoryId | 路径 | Long | 是 | 岗位分类ID |
  | page | 查询 | Integer | 否 | 页码，从0开始，默认0 |
  | size | 查询 | Integer | 否 | 每页大小，默认20 |
- **功能**: 根据分类查询岗位

#### 根据城市查询岗位
- **URL**: `GET /city/{workCity}`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | workCity | 路径 | String | 是 | 工作城市 |
  | page | 查询 | Integer | 否 | 页码，从0开始，默认0 |
  | size | 查询 | Integer | 否 | 每页大小，默认20 |
- **功能**: 根据城市查询岗位

#### 根据类型查询岗位
- **URL**: `GET /type/{jobType}`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | jobType | 路径 | Integer | 是 | 岗位类型：1-全职，2-兼职，3-实习 |
  | page | 查询 | Integer | 否 | 页码，从0开始，默认0 |
  | size | 查询 | Integer | 否 | 每页大小，默认20 |
- **功能**: 根据类型查询岗位

#### 搜索岗位
- **URL**: `GET /search`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | keyword | 查询 | String | 是 | 搜索关键词 |
  | page | 查询 | Integer | 否 | 页码，从0开始，默认0 |
  | size | 查询 | Integer | 否 | 每页大小，默认20 |
- **响应参数**:
  ```json
  {
    "code": 200,
    "message": "搜索成功",
    "data": {
      "content": [
        {
          "id": 1,
          "title": "岗位标题",
          "companyName": "公司名称",
          "jobType": 1,
          "salaryMin": 8000,
          "salaryMax": 15000,
          "workCity": "工作城市"
        }
      ],
      "totalElements": 50,
      "totalPages": 3,
      "size": 20,
      "number": 0
    }
  }
  ```
- **功能**: 搜索岗位

#### 多条件筛选岗位
- **URL**: `GET /filter`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | categoryId | 查询 | Long | 否 | 岗位分类ID |
  | workCity | 查询 | String | 否 | 工作城市 |
  | jobType | 查询 | Integer | 否 | 岗位类型：1-全职，2-兼职，3-实习 |
  | minSalary | 查询 | Integer | 否 | 最低薪资 |
  | maxSalary | 查询 | Integer | 否 | 最高薪资 |
  | page | 查询 | Integer | 否 | 页码，从0开始，默认0 |
  | size | 查询 | Integer | 否 | 每页大小，默认20 |
- **响应参数**:
  ```json
  {
    "code": 200,
    "message": "筛选成功",
    "data": {
      "content": [
        {
          "id": 1,
          "title": "岗位标题",
          "categoryId": 1,
          "companyName": "公司名称",
          "jobType": 1,
          "salaryMin": 8000,
          "salaryMax": 15000,
          "workCity": "工作城市"
        }
      ],
      "totalElements": 30,
      "totalPages": 2,
      "size": 20,
      "number": 0
    }
  }
  ```
- **功能**: 多条件筛选岗位

#### 发布岗位
- **URL**: `POST /{id}/publish`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | id | 路径 | Long | 是 | 岗位ID |
- **功能**: 发布岗位

#### 下架岗位
- **URL**: `POST /{id}/offline`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | id | 路径 | Long | 是 | 岗位ID |
- **功能**: 下架岗位

#### 归档岗位
- **URL**: `POST /{id}/archive`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | id | 路径 | Long | 是 | 岗位ID |
- **功能**: 归档岗位

#### 获取活跃岗位
- **URL**: `GET /active`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | page | 查询 | Integer | 否 | 页码，从0开始，默认0 |
  | size | 查询 | Integer | 否 | 每页大小，默认20 |
- **功能**: 获取活跃岗位

#### 获取即将过期岗位
- **URL**: `GET /expiring`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | page | 查询 | Integer | 否 | 页码，从0开始，默认0 |
  | size | 查询 | Integer | 否 | 每页大小，默认20 |
- **功能**: 获取即将过期岗位

#### 统计岗位状态
- **URL**: `GET /stats/status`
- **功能**: 统计各状态岗位数量

#### 统计城市岗位
- **URL**: `GET /stats/city`
- **功能**: 统计各城市岗位数量

#### 获取热门岗位
- **URL**: `GET /hot`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | limit | 查询 | Integer | 否 | 限制数量，默认10 |
- **功能**: 获取热门岗位

#### 获取最新岗位
- **URL**: `GET /latest`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | limit | 查询 | Integer | 否 | 限制数量，默认10 |
- **功能**: 获取最新岗位

### 3. 简历管理模块 (ResumeController)
**基础路径**: `/api/resumes`

#### 创建简历
- **URL**: `POST /`
- **请求参数**:
  ```json
  {
    "userId": 1,
    "title": "简历标题",
    "content": "JSON格式的简历内容",
    "fileUrl": "文件URL",
    "fileType": "pdf", // pdf/doc/docx
    "isDefault": false,
    "privacyLevel": 1 // 1-公开，2-仅投递企业，3-隐藏
  }
  ```
- **响应参数**:
  ```json
  {
    "code": 200,
    "message": "创建成功",
    "data": {
      "id": 1,
      "userId": 1,
      "title": "简历标题",
      "fileType": "pdf",
      "isDefault": false,
      "privacyLevel": 1
    }
  }
  ```
- **功能**: 创建简历

#### 更新简历
- **URL**: `PUT /{id}`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | id | 路径 | Long | 是 | 简历ID |
  | ResumeDTO | 请求体 | Object | 是 | 简历信息对象 |
- **功能**: 更新简历

#### 删除简历
- **URL**: `DELETE /{id}`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | id | 路径 | Long | 是 | 简历ID |
- **功能**: 删除简历

#### 获取简历详情
- **URL**: `GET /{id}`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | id | 路径 | Long | 是 | 简历ID |
- **功能**: 获取简历详情

#### 分页查询简历列表
- **URL**: `GET /`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | page | 查询 | Integer | 否 | 页码，从0开始，默认0 |
  | size | 查询 | Integer | 否 | 每页大小，默认20 |
  | sort | 查询 | String | 否 | 排序字段，如：createTime |
  | direction | 查询 | String | 否 | 排序方向：ASC/DESC，默认DESC |
- **功能**: 分页查询简历

#### 根据用户ID查询简历
- **URL**: `GET /user/{userId}`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | userId | 路径 | Long | 是 | 用户ID |
- **功能**: 根据用户ID查询简历

#### 根据用户ID分页查询简历
- **URL**: `GET /user/{userId}/page`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | userId | 路径 | Long | 是 | 用户ID |
  | page | 查询 | Integer | 否 | 页码，从0开始，默认0 |
  | size | 查询 | Integer | 否 | 每页大小，默认20 |
- **功能**: 根据用户ID分页查询简历

#### 获取默认简历
- **URL**: `GET /user/{userId}/default`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | userId | 路径 | Long | 是 | 用户ID |
- **功能**: 获取用户默认简历

#### 设置默认简历
- **URL**: `POST /{id}/set-default`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | id | 路径 | Long | 是 | 简历ID |
- **功能**: 设置默认简历

#### 根据隐私级别查询简历
- **URL**: `GET /privacy/{privacyLevel}`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | privacyLevel | 路径 | Integer | 是 | 隐私级别：1-公开，2-仅投递企业，3-隐藏 |
  | page | 查询 | Integer | 否 | 页码，从0开始，默认0 |
  | size | 查询 | Integer | 否 | 每页大小，默认20 |
- **功能**: 根据隐私级别查询简历

#### 根据文件类型查询简历
- **URL**: `GET /file-type/{fileType}`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | fileType | 路径 | String | 是 | 文件类型：pdf/doc/docx |
  | page | 查询 | Integer | 否 | 页码，从0开始，默认0 |
  | size | 查询 | Integer | 否 | 每页大小，默认20 |
- **功能**: 根据文件类型查询简历

#### 搜索简历
- **URL**: `GET /search`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | keyword | 查询 | String | 是 | 搜索关键词 |
  | page | 查询 | Integer | 否 | 页码，从0开始，默认0 |
  | size | 查询 | Integer | 否 | 每页大小，默认20 |
- **功能**: 搜索简历

#### 更新隐私级别
- **URL**: `PUT /{id}/privacy`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | id | 路径 | Long | 是 | 简历ID |
  | privacyLevel | 请求体 | Integer | 是 | 隐私级别：1-公开，2-仅投递企业，3-隐藏 |
- **功能**: 更新隐私级别

#### 获取公开简历
- **URL**: `GET /public`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | page | 查询 | Integer | 否 | 页码，从0开始，默认0 |
  | size | 查询 | Integer | 否 | 每页大小，默认20 |
- **功能**: 获取公开简历

#### 获取热门简历
- **URL**: `GET /hot`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | limit | 查询 | Integer | 否 | 限制数量，默认10 |
- **功能**: 获取热门简历

#### 统计用户简历数量
- **URL**: `GET /user/{userId}/count`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | userId | 路径 | Long | 是 | 用户ID |
- **功能**: 统计用户简历数量

#### 验证简历归属
- **URL**: `GET /{id}/belongs-to/{userId}`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | id | 路径 | Long | 是 | 简历ID |
  | userId | 路径 | Long | 是 | 用户ID |
- **功能**: 验证简历是否属于用户

### 4. 岗位申请模块 (JobApplyController)
**基础路径**: `/api/job-applies`

#### 创建岗位申请
- **URL**: `POST /`
- **请求参数**:
  ```json
  {
    "userId": 1,
    "jobId": 1,
    "resumeId": 1,
    "applyNotes": "申请备注",
    "status": 1 // 1-已投递，2-已查看，3-已通知面试，4-面试通过，5-已拒绝
  }
  ```
- **响应参数**:
  ```json
  {
    "code": 200,
    "message": "申请成功",
    "data": {
      "id": 1,
      "userId": 1,
      "jobId": 1,
      "resumeId": 1,
      "status": 1,
      "applyNotes": "申请备注",
      "matchScore": 85.5
    }
  }
  ```
- **功能**: 创建岗位申请

#### 更新岗位申请
- **URL**: `PUT /{id}`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | id | 路径 | Long | 是 | 申请ID |
  | JobApplyDTO | 请求体 | Object | 是 | 申请信息对象 |
- **功能**: 更新岗位申请

#### 删除岗位申请
- **URL**: `DELETE /{id}`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | id | 路径 | Long | 是 | 申请ID |
- **功能**: 删除岗位申请

#### 获取申请详情
- **URL**: `GET /{id}`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | id | 路径 | Long | 是 | 申请ID |
- **功能**: 获取申请详情

#### 分页查询申请列表
- **URL**: `GET /`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | page | 查询 | Integer | 否 | 页码，从0开始，默认0 |
  | size | 查询 | Integer | 否 | 每页大小，默认20 |
  | sort | 查询 | String | 否 | 排序字段，如：createTime |
  | direction | 查询 | String | 否 | 排序方向：ASC/DESC，默认DESC |
- **功能**: 分页查询申请

#### 根据用户ID查询申请
- **URL**: `GET /user/{userId}`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | userId | 路径 | Long | 是 | 用户ID |
- **功能**: 根据用户ID查询申请

#### 根据用户ID分页查询申请
- **URL**: `GET /user/{userId}/page`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | userId | 路径 | Long | 是 | 用户ID |
  | page | 查询 | Integer | 否 | 页码，从0开始，默认0 |
  | size | 查询 | Integer | 否 | 每页大小，默认20 |
- **功能**: 根据用户ID分页查询申请

#### 根据岗位ID查询申请
- **URL**: `GET /job/{jobId}`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | jobId | 路径 | Long | 是 | 岗位ID |
- **功能**: 根据岗位ID查询申请

#### 根据岗位ID分页查询申请
- **URL**: `GET /job/{jobId}/page`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | jobId | 路径 | Long | 是 | 岗位ID |
  | page | 查询 | Integer | 否 | 页码，从0开始，默认0 |
  | size | 查询 | Integer | 否 | 每页大小，默认20 |
- **功能**: 根据岗位ID分页查询申请

#### 根据简历ID查询申请
- **URL**: `GET /resume/{resumeId}`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | resumeId | 路径 | Long | 是 | 简历ID |
- **功能**: 根据简历ID查询申请

#### 根据状态查询申请
- **URL**: `GET /status/{status}`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | status | 路径 | Integer | 是 | 申请状态：1-已投递，2-已查看，3-已通知面试，4-面试通过，5-已拒绝 |
  | page | 查询 | Integer | 否 | 页码，从0开始，默认0 |
  | size | 查询 | Integer | 否 | 每页大小，默认20 |
- **功能**: 根据状态查询申请

#### 根据用户和状态查询申请
- **URL**: `GET /user/{userId}/status/{status}`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | userId | 路径 | Long | 是 | 用户ID |
  | status | 路径 | Integer | 是 | 申请状态：1-已投递，2-已查看，3-已通知面试，4-面试通过，5-已拒绝 |
  | page | 查询 | Integer | 否 | 页码，从0开始，默认0 |
  | size | 查询 | Integer | 否 | 每页大小，默认20 |
- **功能**: 根据用户和状态查询申请

#### 根据岗位和状态查询申请
- **URL**: `GET /job/{jobId}/status/{status}`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | jobId | 路径 | Long | 是 | 岗位ID |
  | status | 路径 | Integer | 是 | 申请状态：1-已投递，2-已查看，3-已通知面试，4-面试通过，5-已拒绝 |
  | page | 查询 | Integer | 否 | 页码，从0开始，默认0 |
  | size | 查询 | Integer | 否 | 每页大小，默认20 |
- **功能**: 根据岗位和状态查询申请

#### 更新申请状态
- **URL**: `PUT /{id}/status`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | id | 路径 | Long | 是 | 申请ID |
  | status | 请求体 | Integer | 是 | 申请状态：1-已投递，2-已查看，3-已通知面试，4-面试通过，5-已拒绝 |
- **功能**: 更新申请状态

#### 设置面试时间
- **URL**: `PUT /{id}/interview`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | id | 路径 | Long | 是 | 申请ID |
  | interviewTime | 请求体 | String | 是 | 面试时间，ISO 8601格式 |
  | interviewLocation | 请求体 | String | 是 | 面试地点 |
- **功能**: 设置面试时间

#### 添加申请反馈
- **URL**: `PUT /{id}/feedback`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | id | 路径 | Long | 是 | 申请ID |
  | feedback | 请求体 | String | 是 | 申请反馈内容 |
- **功能**: 添加申请反馈

#### 计算匹配度
- **URL**: `GET /match-score`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | jobId | 查询 | Long | 是 | 岗位ID |
  | resumeId | 查询 | Long | 是 | 简历ID |
- **功能**: 计算匹配度

#### 检查是否已申请
- **URL**: `GET /check-apply`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | userId | 查询 | Long | 是 | 用户ID |
  | jobId | 查询 | Long | 是 | 岗位ID |
- **功能**: 检查用户是否已申请岗位

#### 统计申请状态
- **URL**: `GET /stats/status`
- **功能**: 统计各状态申请数量

#### 统计用户申请数量
- **URL**: `GET /user/{userId}/count`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | userId | 路径 | Long | 是 | 用户ID |
- **功能**: 统计用户申请数量

#### 统计岗位申请数量
- **URL**: `GET /job/{jobId}/count`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | jobId | 路径 | Long | 是 | 岗位ID |
- **功能**: 统计岗位申请数量

#### 获取热门申请岗位
- **URL**: `GET /hot-jobs`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | limit | 查询 | Integer | 否 | 限制数量，默认10 |
- **功能**: 获取热门申请岗位

#### 获取用户申请统计
- **URL**: `GET /user/{userId}/stats`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | userId | 路径 | Long | 是 | 用户ID |
- **功能**: 获取用户申请统计

### 5. 通知管理模块 (NotificationController)
**基础路径**: `/api/notifications`

#### 创建通知
- **URL**: `POST /`
- **请求参数**:
  ```json
  {
    "userId": 1,
    "title": "通知标题",
    "content": "通知内容",
    "type": 1, // 1-系统通知，2-申请状态通知，3-面试通知，4-消息提醒
    "status": 1, // 1-未读，2-已读
    "relatedId": "关联业务ID",
    "relatedType": "关联业务类型"
  }
  ```
- **响应参数**:
  ```json
  {
    "code": 200,
    "message": "创建成功",
    "data": {
      "id": 1,
      "userId": 1,
      "title": "通知标题",
      "content": "通知内容",
      "type": 1,
      "status": 1
    }
  }
  ```
- **功能**: 创建通知

#### 批量创建通知
- **URL**: `POST /batch`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | notifications | 请求体 | List<NotificationDTO> | 是 | 通知对象列表 |
- **功能**: 批量创建通知

#### 更新通知
- **URL**: `PUT /{id}`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | id | 路径 | Long | 是 | 通知ID |
  | NotificationDTO | 请求体 | Object | 是 | 通知信息对象 |
- **功能**: 更新通知

#### 删除通知
- **URL**: `DELETE /{id}`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | id | 路径 | Long | 是 | 通知ID |
- **功能**: 删除通知

#### 获取通知详情
- **URL**: `GET /{id}`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | id | 路径 | Long | 是 | 通知ID |
- **功能**: 获取通知详情

#### 分页查询通知列表
- **URL**: `GET /`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | page | 查询 | Integer | 否 | 页码，从0开始，默认0 |
  | size | 查询 | Integer | 否 | 每页大小，默认20 |
  | sort | 查询 | String | 否 | 排序字段，如：createTime |
  | direction | 查询 | String | 否 | 排序方向：ASC/DESC，默认DESC |
- **功能**: 分页查询通知

#### 根据用户ID查询通知
- **URL**: `GET /user/{userId}`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | userId | 路径 | Long | 是 | 用户ID |
- **功能**: 根据用户ID查询通知

#### 根据用户ID分页查询通知
- **URL**: `GET /user/{userId}/page`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | userId | 路径 | Long | 是 | 用户ID |
  | page | 查询 | Integer | 否 | 页码，从0开始，默认0 |
  | size | 查询 | Integer | 否 | 每页大小，默认20 |
- **功能**: 根据用户ID分页查询通知

#### 根据类型查询通知
- **URL**: `GET /type/{type}`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | type | 路径 | Integer | 是 | 通知类型：1-系统通知，2-申请状态通知，3-面试通知，4-消息提醒 |
  | page | 查询 | Integer | 否 | 页码，从0开始，默认0 |
  | size | 查询 | Integer | 否 | 每页大小，默认20 |
- **功能**: 根据类型查询通知

#### 根据状态查询通知
- **URL**: `GET /status/{status}`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | status | 路径 | Integer | 是 | 通知状态：1-未读，2-已读 |
  | page | 查询 | Integer | 否 | 页码，从0开始，默认0 |
  | size | 查询 | Integer | 否 | 每页大小，默认20 |
- **功能**: 根据状态查询通知

#### 根据用户和状态查询通知
- **URL**: `GET /user/{userId}/status/{status}`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | userId | 路径 | Long | 是 | 用户ID |
  | status | 路径 | Integer | 是 | 通知状态：1-未读，2-已读 |
  | page | 查询 | Integer | 否 | 页码，从0开始，默认0 |
  | size | 查询 | Integer | 否 | 每页大小，默认20 |
- **功能**: 根据用户和状态查询通知

#### 根据用户和类型查询通知
- **URL**: `GET /user/{userId}/type/{type}`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | userId | 路径 | Long | 是 | 用户ID |
  | type | 路径 | Integer | 是 | 通知类型：1-系统通知，2-申请状态通知，3-面试通知，4-消息提醒 |
  | page | 查询 | Integer | 否 | 页码，从0开始，默认0 |
  | size | 查询 | Integer | 否 | 每页大小，默认20 |
- **功能**: 根据用户和类型查询通知

#### 标记为已读
- **URL**: `PUT /{id}/read`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | id | 路径 | Long | 是 | 通知ID |
- **功能**: 标记通知为已读

#### 批量标记为已读
- **URL**: `PUT /batch-read`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | notificationIds | 请求体 | List<Long> | 是 | 通知ID列表 |
- **功能**: 批量标记通知为已读

#### 标记所有为已读
- **URL**: `PUT /user/{userId}/read-all`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | userId | 路径 | Long | 是 | 用户ID |
- **功能**: 标记用户所有通知为已读

#### 获取未读通知数量
- **URL**: `GET /user/{userId}/unread-count`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | userId | 路径 | Long | 是 | 用户ID |
- **功能**: 获取用户未读通知数量

#### 获取最新通知
- **URL**: `GET /user/{userId}/latest`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | userId | 路径 | Long | 是 | 用户ID |
  | limit | 查询 | Integer | 否 | 限制数量，默认10 |
- **功能**: 获取用户最新通知

#### 发送申请状态通知
- **URL**: `POST /apply-status`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | userId | 请求体 | Long | 是 | 用户ID |
  | applyId | 请求体 | Long | 是 | 申请ID |
  | status | 请求体 | Integer | 是 | 申请状态：1-已投递，2-已查看，3-已通知面试，4-面试通过，5-已拒绝 |
- **功能**: 发送申请状态通知

#### 发送面试通知
- **URL**: `POST /interview`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | userId | 请求体 | Long | 是 | 用户ID |
  | applyId | 请求体 | Long | 是 | 申请ID |
  | interviewTime | 请求体 | String | 是 | 面试时间，ISO 8601格式 |
  | location | 请求体 | String | 是 | 面试地点 |
- **功能**: 发送面试通知

#### 发送系统通知
- **URL**: `POST /system`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | userId | 请求体 | Long | 是 | 用户ID |
  | title | 请求体 | String | 是 | 通知标题 |
  | content | 请求体 | String | 是 | 通知内容 |
- **功能**: 发送系统通知

#### 删除用户所有通知
- **URL**: `DELETE /user/{userId}`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | userId | 路径 | Long | 是 | 用户ID |
- **功能**: 删除用户所有通知

#### 获取通知统计
- **URL**: `GET /user/{userId}/stats`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | userId | 路径 | Long | 是 | 用户ID |
- **功能**: 获取通知统计信息

### 6. 岗位分类模块 (JobCategoryController)
**基础路径**: `/api/job-categories`

#### 创建岗位分类
- **URL**: `POST /`
- **请求参数**:
  ```json
  {
    "name": "分类名称",
    "description": "分类描述",
    "parentId": 0,
    "sortOrder": 1,
    "status": 1,
    "icon": "分类图标",
    "color": "分类颜色"
  }
  ```
- **功能**: 创建岗位分类

#### 更新岗位分类
- **URL**: `PUT /{id}`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | id | 路径 | Long | 是 | 分类ID |
  | JobCategoryDTO | 请求体 | Object | 是 | 分类信息对象 |
- **功能**: 更新岗位分类

#### 删除岗位分类
- **URL**: `DELETE /{id}`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | id | 路径 | Long | 是 | 分类ID |
- **功能**: 删除岗位分类

#### 获取分类详情
- **URL**: `GET /{id}`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | id | 路径 | Long | 是 | 分类ID |
- **功能**: 获取分类详情

#### 分页查询分类列表
- **URL**: `GET /`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | page | 查询 | Integer | 否 | 页码，从0开始，默认0 |
  | size | 查询 | Integer | 否 | 每页大小，默认20 |
  | sort | 查询 | String | 否 | 排序字段，如：sortOrder |
  | direction | 查询 | String | 否 | 排序方向：ASC/DESC，默认ASC |
- **功能**: 分页查询分类

#### 获取所有分类列表
- **URL**: `GET /all`
- **功能**: 获取所有分类

#### 根据父分类查询子分类
- **URL**: `GET /parent/{parentId}`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | parentId | 路径 | Long | 是 | 父分类ID |
- **功能**: 根据父分类查询子分类

#### 获取顶级分类
- **URL**: `GET /top-level`
- **功能**: 获取顶级分类

#### 根据状态查询分类
- **URL**: `GET /status/{status}`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | status | 路径 | Integer | 是 | 分类状态：1-启用，2-禁用 |
- **功能**: 根据状态查询分类

#### 搜索分类
- **URL**: `GET /search`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | name | 查询 | String | 是 | 分类名称关键词 |
  | page | 查询 | Integer | 否 | 页码，从0开始，默认0 |
  | size | 查询 | Integer | 否 | 每页大小，默认20 |
- **功能**: 根据名称搜索分类

#### 更新分类状态
- **URL**: `PUT /{id}/status`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | id | 路径 | Long | 是 | 分类ID |
  | status | 请求体 | Integer | 是 | 分类状态：1-启用，2-禁用 |
- **功能**: 更新分类状态

#### 更新排序序号
- **URL**: `PUT /{id}/sort-order`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | id | 路径 | Long | 是 | 分类ID |
  | sortOrder | 请求体 | Integer | 是 | 排序序号 |
- **功能**: 更新排序序号

#### 获取分类树
- **URL**: `GET /tree`
- **功能**: 获取分类树形结构

#### 获取分类路径
- **URL**: `GET /{id}/path`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | id | 路径 | Long | 是 | 分类ID |
- **功能**: 获取分类路径

#### 统计岗位数量
- **URL**: `GET /stats/job-count`
- **功能**: 统计各分类下的岗位数量

#### 获取热门分类
- **URL**: `GET /hot`
- **功能**: 获取热门分类

#### 检查名称是否存在
- **URL**: `GET /check-name`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | name | 查询 | String | 是 | 分类名称 |
  | excludeId | 查询 | Long | 否 | 排除的分类ID |
- **功能**: 检查分类名称是否已存在

#### 获取启用分类
- **URL**: `GET /enabled`
- **功能**: 获取启用的分类列表

#### 根据层级查询分类
- **URL**: `GET /level/{level}`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | level | 路径 | Integer | 是 | 分类层级 |
- **功能**: 根据层级查询分类

#### 批量更新分类状态
- **URL**: `PUT /batch-status`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | ids | 请求体 | List<Long> | 是 | 分类ID列表 |
  | status | 请求体 | Integer | 是 | 分类状态：1-启用，2-禁用 |
- **功能**: 批量更新分类状态

#### 获取分类统计信息
- **URL**: `GET /stats`
- **功能**: 获取分类统计信息

### 7. 智能推荐模块 (RecommendationController)
**基础路径**: `/api/recommendations`

#### 为用户推荐岗位
- **URL**: `GET /jobs/user/{userId}`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | userId | 路径 | Long | 是 | 用户ID |
  | limit | 查询 | Integer | 否 | 推荐数量限制，默认10 |
- **功能**: 为用户推荐岗位

#### 为岗位推荐匹配的简历
- **URL**: `GET /resumes/job/{jobId}`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | jobId | 路径 | Long | 是 | 岗位ID |
  | limit | 查询 | Integer | 否 | 推荐数量限制，默认10 |
- **功能**: 为岗位推荐匹配的简历

#### 基于协同过滤的岗位推荐
- **URL**: `GET /collaborative/{userId}`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | userId | 路径 | Long | 是 | 用户ID |
  | limit | 查询 | Integer | 否 | 推荐数量限制，默认10 |
- **功能**: 基于协同过滤的岗位推荐

#### 基于内容的岗位推荐
- **URL**: `GET /content/{userId}`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | userId | 路径 | Long | 是 | 用户ID |
  | limit | 查询 | Integer | 否 | 推荐数量限制，默认10 |
- **功能**: 基于内容的岗位推荐

#### 基于用户画像的岗位推荐
- **URL**: `GET /profile/{userId}`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | userId | 路径 | Long | 是 | 用户ID |
  | limit | 查询 | Integer | 否 | 推荐数量限制，默认10 |
- **功能**: 基于用户画像的岗位推荐

#### 热门岗位推荐
- **URL**: `GET /hot`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | limit | 查询 | Integer | 否 | 推荐数量限制，默认10 |
- **功能**: 热门岗位推荐

#### 最新岗位推荐
- **URL**: `GET /latest`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | limit | 查询 | Integer | 否 | 推荐数量限制，默认10 |
- **功能**: 最新岗位推荐

#### 相似岗位推荐
- **URL**: `GET /similar/{jobId}`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | jobId | 路径 | Long | 是 | 岗位ID |
  | limit | 查询 | Integer | 否 | 推荐数量限制，默认10 |
- **功能**: 相似岗位推荐

#### 基于地理位置的岗位推荐
- **URL**: `GET /location/{userId}`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | userId | 路径 | Long | 是 | 用户ID |
  | limit | 查询 | Integer | 否 | 推荐数量限制，默认10 |
- **功能**: 基于地理位置的岗位推荐

#### 基于薪资期望的岗位推荐
- **URL**: `GET /salary/{userId}`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | userId | 路径 | Long | 是 | 用户ID |
  | limit | 查询 | Integer | 否 | 推荐数量限制，默认10 |
- **功能**: 基于薪资期望的岗位推荐

#### 基于技能匹配的岗位推荐
- **URL**: `GET /skill/{userId}`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | userId | 路径 | Long | 是 | 用户ID |
  | limit | 查询 | Integer | 否 | 推荐数量限制，默认10 |
- **功能**: 基于技能匹配的岗位推荐

#### 混合推荐算法
- **URL**: `GET /hybrid/{userId}`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | userId | 路径 | Long | 是 | 用户ID |
  | limit | 查询 | Integer | 否 | 推荐数量限制，默认10 |
- **功能**: 混合推荐算法

#### 计算岗位与用户的匹配度
- **URL**: `POST /match-scores/user/{userId}`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | userId | 路径 | Long | 是 | 用户ID |
  | jobIds | 请求体 | List<Long> | 是 | 岗位ID列表 |
- **功能**: 计算岗位与用户的匹配度

#### 计算简历与岗位的匹配度
- **URL**: `POST /match-scores/job/{jobId}`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | jobId | 路径 | Long | 是 | 岗位ID |
  | resumeIds | 请求体 | List<Long> | 是 | 简历ID列表 |
- **功能**: 计算简历与岗位的匹配度

#### 获取用户推荐偏好
- **URL**: `GET /preferences/{userId}`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | userId | 路径 | Long | 是 | 用户ID |
- **功能**: 获取用户推荐偏好

#### 更新用户推荐偏好
- **URL**: `PUT /preferences/{userId}`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | userId | 路径 | Long | 是 | 用户ID |
  | preferences | 请求体 | Object | 是 | 推荐偏好对象 |
- **功能**: 更新用户推荐偏好

#### 获取推荐系统统计信息
- **URL**: `GET /stats`
- **功能**: 获取推荐系统统计信息

#### 训练推荐模型
- **URL**: `POST /train`
- **功能**: 训练推荐模型

#### 清除推荐缓存
- **URL**: `DELETE /cache/{userId}`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | userId | 路径 | Long | 是 | 用户ID |
- **功能**: 清除推荐缓存

#### 获取推荐解释
- **URL**: `POST /explanation/{userId}`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | userId | 路径 | Long | 是 | 用户ID |
  | jobIds | 请求体 | List<Long> | 是 | 岗位ID列表 |
- **功能**: 获取推荐解释

#### 评估推荐效果
- **URL**: `GET /performance`
- **功能**: 评估推荐效果

### 8. 数据分析模块 (AnalyticsController)
**基础路径**: `/api/analytics`

#### 获取用户行为分析
- **URL**: `GET /user-behavior/{userId}`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | userId | 路径 | Long | 是 | 用户ID |
  | startTime | 查询 | String | 否 | 开始时间，ISO 8601格式 |
  | endTime | 查询 | String | 否 | 结束时间，ISO 8601格式 |
- **功能**: 获取用户行为分析

#### 获取岗位热度分析
- **URL**: `GET /job-heat/{jobId}`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | jobId | 路径 | Long | 是 | 岗位ID |
  | period | 查询 | String | 否 | 分析周期：day/week/month |
- **功能**: 获取岗位热度分析

#### 获取申请成功率分析
- **URL**: `GET /apply-success-rate/{userId}`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | userId | 路径 | Long | 是 | 用户ID |
  | startTime | 查询 | String | 否 | 开始时间，ISO 8601格式 |
  | endTime | 查询 | String | 否 | 结束时间，ISO 8601格式 |
- **功能**: 获取申请成功率分析

#### 获取系统整体统计
- **URL**: `GET /system-statistics`
- **功能**: 获取系统整体统计

#### 获取用户增长趋势
- **URL**: `GET /user-growth-trend`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | startTime | 查询 | String | 是 | 开始时间，ISO 8601格式 |
  | endTime | 查询 | String | 是 | 结束时间，ISO 8601格式 |
  | userType | 查询 | Integer | 否 | 用户类型：1-求职者，2-企业用户，3-管理员 |
- **功能**: 获取用户增长趋势

#### 获取岗位发布趋势
- **URL**: `GET /job-publish-trend`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | startTime | 查询 | String | 是 | 开始时间，ISO 8601格式 |
  | endTime | 查询 | String | 是 | 结束时间，ISO 8601格式 |
  | jobType | 查询 | Integer | 否 | 岗位类型：1-全职，2-兼职，3-实习 |
- **功能**: 获取岗位发布趋势

#### 获取申请趋势分析
- **URL**: `GET /apply-trend`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | startTime | 查询 | String | 是 | 开始时间，ISO 8601格式 |
  | endTime | 查询 | String | 是 | 结束时间，ISO 8601格式 |
  | status | 查询 | Integer | 否 | 申请状态：1-已投递，2-已查看，3-已通知面试，4-面试通过，5-已拒绝 |
- **功能**: 获取申请趋势分析

#### 获取热门技能分析
- **URL**: `GET /hot-skills`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | limit | 查询 | Integer | 否 | 限制数量，默认10 |
  | period | 查询 | String | 否 | 分析周期：day/week/month |
- **功能**: 获取热门技能分析

#### 获取热门城市分析
- **URL**: `GET /hot-cities`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | limit | 查询 | Integer | 否 | 限制数量，默认10 |
  | period | 查询 | String | 否 | 分析周期：day/week/month |
- **功能**: 获取热门城市分析

#### 获取薪资分布分析
- **URL**: `GET /salary-distribution`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | jobType | 查询 | Integer | 否 | 岗位类型：1-全职，2-兼职，3-实习 |
  | workCity | 查询 | String | 否 | 工作城市 |
- **功能**: 获取薪资分布分析

#### 获取用户画像分析
- **URL**: `GET /user-profile`
- **功能**: 获取用户画像分析

#### 获取岗位匹配度分析
- **URL**: `GET /job-match`
- **功能**: 获取岗位匹配度分析

#### 获取活跃用户分析
- **URL**: `GET /active-users`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | days | 查询 | Integer | 是 | 活跃天数阈值 |
  | userType | 查询 | Integer | 否 | 用户类型：1-求职者，2-企业用户，3-管理员 |
- **功能**: 获取活跃用户分析

#### 获取企业招聘分析
- **URL**: `GET /company-recruitment/{companyId}`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | companyId | 路径 | Long | 是 | 企业ID |
  | startTime | 查询 | String | 否 | 开始时间，ISO 8601格式 |
  | endTime | 查询 | String | 否 | 结束时间，ISO 8601格式 |
- **功能**: 获取企业招聘分析

#### 获取简历质量分析
- **URL**: `GET /resume-quality/{userId}`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | userId | 路径 | Long | 是 | 用户ID |
- **功能**: 获取简历质量分析

#### 获取面试通过率分析
- **URL**: `GET /interview-pass-rate`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | startTime | 查询 | String | 否 | 开始时间，ISO 8601格式 |
  | endTime | 查询 | String | 否 | 结束时间，ISO 8601格式 |
  | companyId | 查询 | Long | 否 | 企业ID |
- **功能**: 获取面试通过率分析

#### 获取用户留存分析
- **URL**: `GET /user-retention`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | days | 查询 | Integer | 是 | 留存天数 |
  | userType | 查询 | Integer | 否 | 用户类型：1-求职者，2-企业用户，3-管理员 |
- **功能**: 获取用户留存分析

#### 获取转化率分析
- **URL**: `GET /conversion-rate`
- **功能**: 获取转化率分析

#### 获取推荐效果分析
- **URL**: `GET /recommendation-effect`
- **功能**: 获取推荐效果分析

#### 获取数据质量报告
- **URL**: `GET /data-quality`
- **功能**: 获取数据质量报告

#### 导出分析报告
- **URL**: `POST /export-report`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | reportType | 请求体 | String | 是 | 报告类型：user_behavior/job_heat/apply_trend等 |
  | parameters | 请求体 | Object | 是 | 报告参数对象 |
- **功能**: 导出分析报告

#### 获取实时数据监控
- **URL**: `GET /real-time-monitoring`
- **功能**: 获取实时数据监控

#### 获取异常检测结果
- **URL**: `GET /anomaly-detection`
- **功能**: 获取异常检测结果

#### 获取综合仪表板数据
- **URL**: `GET /dashboard`
- **功能**: 获取综合仪表板数据

#### 获取权限系统统计信息
- **URL**: `GET /permissions/stats`
- **功能**: 获取权限系统统计信息

### 9. 权限管理模块 (PermissionController)
**基础路径**: `/api/permissions`

#### 检查用户权限
- **URL**: `GET /check`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | userId | 查询 | Long | 是 | 用户ID |
  | permission | 查询 | String | 是 | 权限代码 |
- **响应参数**:
  ```json
  {
    "code": 200,
    "message": "检查成功",
    "data": true
  }
  ```
- **功能**: 检查用户是否拥有指定权限

#### 检查用户角色
- **URL**: `GET /check-role`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | userId | 查询 | Long | 是 | 用户ID |
  | role | 查询 | String | 是 | 角色代码 |
- **响应参数**:
  ```json
  {
    "code": 200,
    "message": "检查成功",
    "data": true
  }
  ```
- **功能**: 检查用户是否拥有指定角色

#### 获取用户权限
- **URL**: `GET /user/{userId}`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | userId | 路径 | Long | 是 | 用户ID |
- **响应参数**:
  ```json
  {
    "code": 200,
    "message": "获取成功",
    "data": ["apply:manage", "resume:manage"]
  }
  ```
- **功能**: 获取用户所有权限

#### 获取用户角色
- **URL**: `GET /user/{userId}/roles`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | userId | 路径 | Long | 是 | 用户ID |
- **响应参数**:
  ```json
  {
    "code": 200,
    "message": "获取成功",
    "data": ["job_seeker"]
  }
  ```
- **功能**: 获取用户所有角色

#### 为用户分配角色
- **URL**: `POST /user/{userId}/role`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | userId | 路径 | Long | 是 | 用户ID |
  | roleId | 查询 | Long | 是 | 角色ID |
- **响应参数**:
  ```json
  {
    "code": 200,
    "message": "分配成功",
    "data": true
  }
  ```
- **功能**: 为用户分配角色

#### 移除用户角色
- **URL**: `DELETE /user/{userId}/role`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | userId | 路径 | Long | 是 | 用户ID |
  | roleId | 查询 | Long | 是 | 角色ID |
- **响应参数**:
  ```json
  {
    "code": 200,
    "message": "移除成功",
    "data": true
  }
  ```
- **功能**: 移除用户的角色

#### 为角色分配权限
- **URL**: `POST /role/{roleId}/permission`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | roleId | 路径 | Long | 是 | 角色ID |
  | permissionId | 查询 | Long | 是 | 权限ID |
- **响应参数**:
  ```json
  {
    "code": 200,
    "message": "分配成功",
    "data": true
  }
  ```
- **功能**: 为角色分配权限

#### 移除角色权限
- **URL**: `DELETE /role/{roleId}/permission`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | roleId | 路径 | Long | 是 | 角色ID |
  | permissionId | 查询 | Long | 是 | 权限ID |
- **响应参数**:
  ```json
  {
    "code": 200,
    "message": "移除成功",
    "data": true
  }
  ```
- **功能**: 移除角色的权限

#### 获取角色权限
- **URL**: `GET /role/{roleId}/permissions`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | roleId | 路径 | Long | 是 | 角色ID |
- **响应参数**:
  ```json
  {
    "code": 200,
    "message": "获取成功",
    "data": ["apply:manage", "resume:manage"]
  }
  ```
- **功能**: 获取角色权限列表

#### 获取所有权限
- **URL**: `GET /`
- **响应参数**:
  ```json
  {
    "code": 200,
    "message": "获取成功",
    "data": ["apply:manage", "resume:manage", "job:create"]
  }
  ```
- **功能**: 获取所有可用权限

#### 获取所有角色
- **URL**: `GET /roles`
- **响应参数**:
  ```json
  {
    "code": 200,
    "message": "获取成功",
    "data": ["job_seeker", "employer", "admin"]
  }
  ```
- **功能**: 获取所有可用角色

### 10. 消息推送模块 (MessagePushController)
**基础路径**: `/api/message-push`

#### 发送站内消息
- **URL**: `POST /internal`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | userId | 查询 | Long | 是 | 用户ID |
  | title | 查询 | String | 是 | 消息标题 |
  | content | 查询 | String | 是 | 消息内容 |
  | type | 查询 | Integer | 是 | 消息类型 |
- **响应参数**:
  ```json
  {
    "code": 200,
    "message": "站内消息发送成功",
    "data": {
      "success": true,
      "message": "站内消息发送成功"
    }
  }
  ```
- **功能**: 发送站内消息

#### 批量发送站内消息
- **URL**: `POST /internal/batch`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | userIds | 查询 | List<Long> | 是 | 用户ID列表 |
  | title | 查询 | String | 是 | 消息标题 |
  | content | 查询 | String | 是 | 消息内容 |
  | type | 查询 | Integer | 是 | 消息类型 |
- **响应参数**:
  ```json
  {
    "code": 200,
    "message": "批量站内消息发送成功",
    "data": {
      "success": true,
      "message": "批量站内消息发送成功"
    }
  }
  ```
- **功能**: 批量发送站内消息

#### 发送邮件通知
- **URL**: `POST /email`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | toEmail | 查询 | String | 是 | 收件人邮箱 |
  | subject | 查询 | String | 是 | 邮件主题 |
  | content | 查询 | String | 是 | 邮件内容 |
- **响应参数**:
  ```json
  {
    "code": 200,
    "message": "邮件发送成功",
    "data": {
      "success": true,
      "message": "邮件发送成功"
    }
  }
  ```
- **功能**: 发送邮件通知

#### 发送短信通知
- **URL**: `POST /sms`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | phone | 查询 | String | 是 | 手机号 |
  | content | 查询 | String | 是 | 短信内容 |
- **响应参数**:
  ```json
  {
    "code": 200,
    "message": "短信发送成功",
    "data": {
      "success": true,
      "message": "短信发送成功"
    }
  }
  ```
- **功能**: 发送短信通知

#### 发送申请状态通知
- **URL**: `POST /apply-status`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | userId | 查询 | Long | 是 | 用户ID |
  | applyId | 查询 | Long | 是 | 申请ID |
  | status | 查询 | String | 是 | 申请状态 |
  | jobTitle | 查询 | String | 是 | 岗位标题 |
- **响应参数**:
  ```json
  {
    "code": 200,
    "message": "申请状态通知发送成功",
    "data": {
      "success": true,
      "message": "申请状态通知发送成功"
    }
  }
  ```
- **功能**: 发送申请状态通知

#### 发送面试通知
- **URL**: `POST /interview`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | userId | 查询 | Long | 是 | 用户ID |
  | applyId | 查询 | Long | 是 | 申请ID |
  | interviewTime | 查询 | String | 是 | 面试时间 |
  | location | 查询 | String | 是 | 面试地点 |
  | jobTitle | 查询 | String | 是 | 岗位标题 |
- **响应参数**:
  ```json
  {
    "code": 200,
    "message": "面试通知发送成功",
    "data": {
      "success": true,
      "message": "面试通知发送成功"
    }
  }
  ```
- **功能**: 发送面试通知

#### 发送录用通知
- **URL**: `POST /offer`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|

### 11. 文件管理模块 (FileController)
**基础路径**: `/api/files`

#### 上传文件
- **URL**: `POST /upload`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | file | 表单 | MultipartFile | 是 | 上传的文件 |
  | userId | 表单 | Long | 是 | 用户ID |
  | fileType | 表单 | String | 是 | 文件类型：resume/avatar/id_card/other |
  | description | 表单 | String | 否 | 文件描述 |
  | tags | 表单 | String | 否 | 文件标签，逗号分隔 |
- **响应参数**:
  ```json
  {
    "code": 200,
    "message": "上传成功",
    "data": {
      "id": 1,
      "fileName": "example.pdf",
      "fileSize": 1024000,
      "fileType": "resume",
      "fileUrl": "/files/download/1",
      "uploadTime": "2025-11-08T12:00:00"
    }
  }
  ```
- **功能**: 上传文件

#### 批量上传文件
- **URL**: `POST /batch-upload`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | files | 表单 | List<MultipartFile> | 是 | 上传的文件列表 |
  | userId | 表单 | Long | 是 | 用户ID |
  | fileType | 表单 | String | 是 | 文件类型：resume/avatar/id_card/other |
  | descriptions | 表单 | List<String> | 否 | 文件描述列表 |
- **响应参数**:
  ```json
  {
    "code": 200,
    "message": "批量上传成功",
    "data": {
      "successCount": 5,
      "failedCount": 0,
      "failedFiles": [],
      "uploadedFiles": [
        {
          "id": 1,
          "fileName": "file1.pdf",
          "fileSize": 1024000,
          "fileType": "resume",
          "fileUrl": "/files/download/1"
        }
      ]
    }
  }
  ```
- **功能**: 批量上传文件

#### 下载文件
- **URL**: `GET /download/{id}`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | id | 路径 | Long | 是 | 文件ID |
- **响应参数**: 文件流
- **功能**: 下载文件

#### 获取文件信息
- **URL**: `GET /{id}`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | id | 路径 | Long | 是 | 文件ID |
- **响应参数**:
  ```json
  {
    "code": 200,
    "message": "获取成功",
    "data": {
      "id": 1,
      "fileName": "example.pdf",
      "fileSize": 1024000,
      "fileType": "resume",
      "fileUrl": "/files/download/1",
      "userId": 1,
      "description": "个人简历",
      "tags": "简历,PDF",
      "uploadTime": "2025-11-08T12:00:00",
      "downloadCount": 5,
      "status": 1
    }
  }
  ```
- **功能**: 获取文件信息

#### 删除文件
- **URL**: `DELETE /{id}`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | id | 路径 | Long | 是 | 文件ID |
- **响应参数**:
  ```json
  {
    "code": 200,
    "message": "删除成功",
    "data": null
  }
  ```
- **功能**: 删除文件

#### 获取文件列表
- **URL**: `GET /`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | page | 查询 | Integer | 否 | 页码，从0开始，默认0 |
  | size | 查询 | Integer | 否 | 每页大小，默认20 |
  | sort | 查询 | String | 否 | 排序字段，如：uploadTime |
  | direction | 查询 | String | 否 | 排序方向：ASC/DESC，默认DESC |
- **响应参数**:
  ```json
  {
    "code": 200,
    "message": "获取成功",
    "data": {
      "content": [
        {
          "id": 1,
          "fileName": "example.pdf",
          "fileSize": 1024000,
          "fileType": "resume",
          "fileUrl": "/files/download/1",
          "userId": 1,
          "uploadTime": "2025-11-08T12:00:00",
          "downloadCount": 5
        }
      ],
      "totalElements": 100,
      "totalPages": 5,
      "size": 20,
      "number": 0
    }
  }
  ```
- **功能**: 获取文件列表

#### 根据用户获取文件
- **URL**: `GET /user/{userId}`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | userId | 路径 | Long | 是 | 用户ID |
  | page | 查询 | Integer | 否 | 页码，从0开始，默认0 |
  | size | 查询 | Integer | 否 | 每页大小，默认20 |
  | fileType | 查询 | String | 否 | 文件类型：resume/avatar/id_card/other |
  | status | 查询 | Integer | 否 | 文件状态：1-正常，2-已删除 |
- **响应参数**:
  ```json
  {
    "code": 200,
    "message": "获取成功",
    "data": {
      "content": [
        {
          "id": 1,
          "fileName": "example.pdf",
          "fileSize": 1024000,
          "fileType": "resume",
          "fileUrl": "/files/download/1",
          "uploadTime": "2025-11-08T12:00:00",
          "downloadCount": 5
        }
      ],
      "totalElements": 50,
      "totalPages": 3,
      "size": 20,
      "number": 0
    }
  }
  ```
- **功能**: 根据用户获取文件

#### 根据类型获取文件
- **URL**: `GET /type/{fileType}`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | fileType | 路径 | String | 是 | 文件类型：resume/avatar/id_card/other |
  | page | 查询 | Integer | 否 | 页码，从0开始，默认0 |
  | size | 查询 | Integer | 否 | 每页大小，默认20 |
  | userId | 查询 | Long | 否 | 用户ID |
- **响应参数**:
  ```json
  {
    "code": 200,
    "message": "获取成功",
    "data": {
      "content": [
        {
          "id": 1,
          "fileName": "example.pdf",
          "fileSize": 1024000,
          "fileType": "resume",
          "fileUrl": "/files/download/1",
          "userId": 1,
          "uploadTime": "2025-11-08T12:00:00"
        }
      ],
      "totalElements": 30,
      "totalPages": 2,
      "size": 20,
      "number": 0
    }
  }
  ```
- **功能**: 根据类型获取文件

#### 搜索文件
- **URL**: `GET /search`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | keyword | 查询 | String | 是 | 搜索关键词 |
  | page | 查询 | Integer | 否 | 页码，从0开始，默认0 |
  | size | 查询 | Integer | 否 | 每页大小，默认20 |
  | fileType | 查询 | String | 否 | 文件类型：resume/avatar/id_card/other |
  | userId | 查询 | Long | 否 | 用户ID |
- **响应参数**:
  ```json
  {
    "code": 200,
    "message": "搜索成功",
    "data": {
      "content": [
        {
          "id": 1,
          "fileName": "example.pdf",
          "fileSize": 1024000,
          "fileType": "resume",
          "fileUrl": "/files/download/1",
          "userId": 1,
          "uploadTime": "2025-11-08T12:00:00"
        }
      ],
      "totalElements": 25,
      "totalPages": 2,
      "size": 20,
      "number": 0
    }
  }
  ```
- **功能**: 搜索文件

#### 更新文件信息
- **URL**: `PUT /{id}`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | id | 路径 | Long | 是 | 文件ID |
  | FileDTO | 请求体 | Object | 是 | 文件信息对象 |
- **响应参数**:
  ```json
  {
    "code": 200,
    "message": "更新成功",
    "data": {
      "id": 1,
      "fileName": "example.pdf",
      "description": "更新后的文件描述",
      "tags": "简历,PDF,更新",
      "status": 1
    }
  }
  ```
- **功能**: 更新文件信息

#### 获取文件统计
- **URL**: `GET /stats`
- **响应参数**:
  ```json
  {
    "code": 200,
    "message": "获取成功",
    "data": {
      "totalFiles": 1000,
      "totalSize": 1024000000,
      "fileTypes": {
        "resume": 500,
        "avatar": 200,
        "id_card": 100,
        "other": 200
      },
      "dailyUploadCount": 50,
      "weeklyUploadCount": 350,
      "monthlyUploadCount": 1000,
      "totalDownloadCount": 5000,
      "averageFileSize": 1024000
    }
  }
  ```
- **功能**: 获取文件统计

#### 清理临时文件
- **URL**: `DELETE /cleanup-temp`
- **请求参数**:
  | 参数名 | 位置 | 数据类型 | 必填 | 说明 |
  |--------|------|----------|------|------|
  | days | 查询 | Integer | 否 | 清理多少天前的临时文件，默认7天 |
- **响应参数**:
  ```json
  {
    "code": 200,
    "message": "清理成功",
    "data": {
      "deletedCount": 50,
      "freedSpace": 51200000
    }
  }
  ```
- **功能**: 清理临时文件

## 测试建议

### 1. 基础功能测试
- 用户注册、登录、资料管理
- 岗位的增删改查和搜索
- 简历的创建和管理
- 岗位申请流程测试

### 2. 高级功能测试
- 智能推荐系统测试
- 数据分析功能测试
- 权限管理系统测试
- 消息推送功能测试

### 3. 性能测试
- 并发用户测试
- 大数据量查询测试
- 文件上传下载性能测试

### 4. 安全测试
- 权限验证测试
- 数据安全测试
- 接口安全测试

## 部署说明

1. 配置数据库连接
2. 运行数据库初始化脚本
3. 启动Spring Boot应用
4. 访问API接口进行测试

## 鉴权说明

### 认证方式
系统使用基于JWT (JSON Web Token) 的Bearer Token认证方式。

#### 1. 获取Token
通过登录接口获取访问令牌：

```bash
# 用户登录获取Token
curl -X POST "http://localhost:8080/api/users/login" \
  -H "Content-Type: application/json" \
  -d '{
    "account": "testuser",
    "password": "password123"
  }'
```

响应示例：
```json
{
  "code": 200,
  "message": "登录成功",
  "data": {
    "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
    "user": {
      "id": 1,
      "username": "testuser",
      "phone": "13800138000",
      "email": "test@example.com",
      "userType": 1,
      "status": 1,
      "authStatus": 0
    }
  }
}
```

#### 2. 使用Token
在后续请求的Header中携带Token：

```bash
curl -X GET "http://localhost:8080/api/jobs" \
  -H "Authorization: Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
```

#### 3. Token过期
- Token默认有效期为24小时
- 过期后需要重新登录获取新的Token
- 可以通过刷新Token接口续期（如果支持）

#### 4. 权限控制
系统根据用户类型和角色进行权限控制：

| 用户类型 | 权限说明 |
|---------|----------|
| 1-求职者 | 可查看岗位、投递简历、管理个人资料 |
| 2-企业用户 | 可发布岗位、查看申请、管理企业信息 |
| 3-管理员 | 拥有所有权限，可管理用户、岗位、系统配置 |

### 无需认证的接口
以下接口无需认证即可访问：
- 用户注册 (`POST /api/users/register`)
- 用户登录 (`POST /api/users/login`)
- 微信登录 (`POST /api/users/login/wx`)
- 手机号登录 (`POST /api/users/login/phone`)
- 发送验证码 (`POST /api/users/send-verification-code`)
- 重置密码 (`POST /api/users/reset-password`)

### 需要认证的接口
除上述登录和认证相关接口外，其他所有接口都需要有效的JWT Token进行鉴权验证，包括但不限于：
- 所有用户管理接口（除登录注册外）
- 所有岗位管理接口
- 所有简历管理接口
- 所有岗位申请接口
- 所有通知管理接口
- 所有权限管理接口
- 所有消息推送接口
- 所有文件管理接口
- 所有推荐系统接口
- 所有数据分析接口

## 接口使用说明

### 请求格式
- **Content-Type**: `application/json`
- **认证方式**: Bearer Token (JWT)
- **分页参数**: 
  - `page`: 页码 (从0开始)
  - `size`: 每页大小
  - `sort`: 排序字段
  - `direction`: 排序方向 (ASC/DESC)

### 响应格式
```json
{
  "code": 200,
  "message": "操作成功",
  "data": {},
  "timestamp": "2025-11-08T11:19:45"
}
```

### 常见状态码
- `200`: 成功
- `400`: 请求参数错误
- `401`: 未授权
- `403`: 权限不足
- `404`: 资源不存在
- `500`: 服务器内部错误

## 注意事项

- 所有API接口都需要进行身份验证（Bearer Token）
- 文件上传需要配置存储路径
- 推荐系统需要训练数据
- 数据分析需要足够的数据量
- 接口调用频率限制：100次/分钟
- 文件大小限制：单个文件不超过10MB
- 分页查询默认页码从0开始，每页大小默认20
- 所有时间参数使用ISO 8601格式：yyyy-MM-dd'T'HH:mm:ss
- 状态码说明：
  - 200: 成功
  - 400: 请求参数错误
  - 401: 未授权
  - 403: 权限不足
  - 404: 资源不存在
  - 500: 服务器内部错误

## 测试账号信息

为了方便测试，系统已预置以下测试账号：

| 用户名 | 密码 | 用户类型 | 说明 |
|--------|------|----------|------|
| wangcheng03 | ABCabc@123 | 求职者 | 主要测试账号 |
| testuser | password123 | 求职者 | 备用测试账号 |
| company1 | password123 | 企业用户 | 企业用户测试账号 |
| company2 | password123 | 企业用户 | 企业用户测试账号 |

**推荐使用测试账号：wangcheng03 / ABCabc@123**

## 快速开始

### 1. 用户注册
```bash
curl -X POST "http://localhost:8080/api/users/register" \
  -H "Content-Type: application/json" \
  -d '{
    "username": "testuser",
    "phone": "13800138000",
    "email": "test@example.com",
    "password": "password123",
    "userType": 1
  }'
```

### 2. 用户登录（使用测试账号）
```bash
curl -X POST "http://localhost:8080/api/users/login" \
  -H "Content-Type: application/json" \
  -d '{
    "account": "wangcheng03",
    "password": "ABCabc@123"
  }'
```

### 3. 获取岗位列表
```bash
curl -X GET "http://localhost:8080/api/jobs?page=0&size=10" \
  -H "Authorization: Bearer {token}"
```

### 4. 创建岗位申请
```bash
curl -X POST "http://localhost:8080/api/job-applies" \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer {token}" \
  -d '{
    "userId": 1,
    "jobId": 1,
    "resumeId": 1,
    "applyNotes": "对该岗位非常感兴趣",
    "status": 1
  }'
```

## 版本信息

- **当前版本**: v1.0.0
- **最后更新**: 2025-11-08
- **维护者**: 就业平台开发团队
