spring.application.name=employment

# MySQL数据库配置（生产环境）
spring.datasource.url=jdbc:mysql://10.26.107.183:3306/cloud1-8gn26bbnb353d1a7?useUnicode=true&characterEncoding=UTF-8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&zeroDateTimeBehavior=CONVERT_TO_NULL
spring.datasource.username=employment
spring.datasource.password=qwe123!@#
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.sql-script-encoding=UTF-8
spring.sql.init.encoding=UTF-8

# JPA配置
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# 服务器配置
server.port=8080

# 安全配置
spring.security.user.name=admin
spring.security.user.password=admin

# 密码编码器配置
spring.security.encoding.strategy=bcrypt

# 数据初始化配置
spring.sql.init.mode=always
spring.jpa.defer-datasource-initialization=true

# 文件上传配置（通过MultipartConfig类动态配置）
spring.servlet.multipart.enabled=true

# 签名URL配置
app.url-signer.secret=6uc885d843nwzfr4wmwe5w8jvtjzfn4e

# 生产环境特定配置
# 关闭开发工具
spring.devtools.restart.enabled=false
# 关闭执行器端点（或限制访问）
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=when-authorized
# 日志配置
logging.level.root=INFO
logging.level.com.shera.framework.employment=DEBUG
logging.file.name=logs/employment.log
logging.pattern.console=%d{yyyy-MM-dd HH:mm:ss} - %msg%n
logging.pattern.file=%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n
